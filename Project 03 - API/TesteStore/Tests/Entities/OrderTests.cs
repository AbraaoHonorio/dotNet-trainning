using Microsoft.VisualStudio.TestTools.UnitTesting;
using System.Collections.Generic;
using TesteStore.Domain.StoreContext.Entities;
using TesteStore.Domain.StoreContext.ValueObjects;

namespace TesteStore.Tests.Entities
{
    [TestClass]
    public class OrderTests
    {    /* TO DO */
        // Consigo Criar um novo pedido
        // Ao criar o pedido, o stats deve ser Creted
        // Ao adicionar um novo item, a quantidade  deve  mudar
        // Ao adicioar um novo item, deve subtrair a quantidade do produto
        // ao confirmar um pedido, deve gerar um número
        // Ao pagar um pedido, o status deve ser pago
        // dados mias de 10 produtos, deve haver duas entregar 
        // Ao cancelar o pedido, o status deve ser cancelado
        // Ao cancelar o epdido, deve cancelar as entregar

    }
}
